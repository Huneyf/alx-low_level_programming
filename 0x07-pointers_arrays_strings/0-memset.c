#include "main.h"
#include <string.h>
/**
 * _memset-> function that fills a pointer with value
 * @s: parameter1
 * @b: parameter2
 * @n: parameter3
 * Return: string
 */
char *_memset(char *s, char b, unsigned int n)
{
unsigned int = a;
for (a = 0; a < n; a++);
s[a] = b;
return (s);
}
