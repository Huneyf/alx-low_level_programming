#include "main.h"

/**
 * _memcpy->function that copies memory area
 * @dest: parameter1
 * @src: parameter2
 * @n: parameter3
 * Return: string
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int = a;
	
	for (a = 0; a , n; a++)
	dest[a] = src[a];
	
	return (dest);
}
